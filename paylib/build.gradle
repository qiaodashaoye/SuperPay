apply plugin: 'com.android.library'

android {
    compileSdkVersion 27
    defaultConfig {
        minSdkVersion 15
        targetSdkVersion 27
        versionCode 1
        versionName "1.0"

        testInstrumentationRunner "android.support.test.runner.AndroidJUnitRunner"

    }
    packagingOptions {
        exclude 'META-INF/DEPENDENCIES'
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }

//    lintOptions {
//        abortOnError false
//    }
}

//publish {
//    userOrg = 'qpg'      //bintray注册的用户名
//    repo="maven"
//    name="SimplePay"
//    groupId = 'com.qpg'         //compile引用时的第1部分groupId
//    artifactId = 'SimplePay'     //compile引用时的第2部分项目名
//    publishVersion = '1.0.0'    //compile引用时的第3部分版本号
//    desc = 'This is a Pay extend library'
//    website = 'https://github.com/qiaodashaoye/SimplePay.git'
//}

dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
    api 'com.android.support:appcompat-v7:27.1.1'
    // compile 'org.apache.httpcomponents.client5:httpclient5:5.0-alpha2'
    api 'com.tencent.mm.opensdk:wechat-sdk-android-without-mta:+'
    api files('libs/httpcore-4.4.6.jar')
    api files('libs/httpclient-4.5.3.jar')
    api files('libs/alipaySdk-20180403.jar')
}

//需添加如下内容

apply plugin: 'com.github.dcendents.android-maven'
apply plugin: 'com.jfrog.bintray'


//发布到组织名称名字，必须填写
group = "com.qpg"
//发布到JCenter上的项目名字，必须填写
def libName = "SimplePay"
// 版本号，下次更新是只需要更改版本号即可
version = "1.0.6"
/**  上面配置后上传至jcenter后的编译路径是这样的： compile       'com.qpg:SimplePay:1.0.0'  **/

//生成源文件
task sourcesJar(type: Jar) {
    from android.sourceSets.main.java.srcDirs
    classifier = 'sources'
}
//生成文档
task javadoc(type: Javadoc) {
    source = android.sourceSets.main.java.srcDirs
    classpath += project.files(android.getBootClasspath().join(File.pathSeparator))
    options.encoding "UTF-8"
    options.charSet 'UTF-8'
    options.author true
    options.version true
    failOnError false
}

//文档打包成jar
task javadocJar(type: Jar, dependsOn: javadoc) {
    classifier = 'javadoc'
    from javadoc.destinationDir
}
//拷贝javadoc文件
task copyDoc(type: Copy) {
    from "${buildDir}/docs/"
    into "docs"
}

//上传到jcenter所需要的源码文件
artifacts {
    archives javadocJar
    archives sourcesJar
}

// 配置maven库，生成POM.xml文件
install {
    repositories.mavenInstaller {
        // This generates POM.xml with proper parameters
        pom {
            project {
                packaging 'aar'
                name 'This is iot sdk'
                developers {
                    developer {
                        id 'SimplePay'
                        name 'qpg'
                        email '1451449315@qq.com'
                    }
                }
            }
        }
    }
}

//上传到jcenter
Properties properties = new Properties()
properties.load(project.rootProject.file('local.properties').newDataInputStream())
bintray {
    user = properties.getProperty("bintray.user")    //读取 local.properties 文件里面的 bintray.user
    key = properties.getProperty("bintray.apikey")   //读取 local.properties 文件里面的 bintray.apikey
    configurations = ['archives']
    pkg {
        userOrg = "qpglibs"
        repo = "maven"
        name = libName    //发布到JCenter上的项目名字，必须填写
        desc = 'This is a iot sdk'    //项目描述
        licenses = ["Apache-2.0"]
        publish = true
    }
}
